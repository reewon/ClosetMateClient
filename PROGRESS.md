# ClosetMate 앱 개발 진행 상황

## ✅ 완료된 작업

### Phase 1: Firebase 인증 및 기본 화면
- [x] Firebase 초기화 (`main.dart`, `firebase_options.dart`)
- [x] Firebase Auth 패키지 설정 (`pubspec.yaml`)
- [x] AuthService 구현 (`lib/services/auth_service.dart`)
  - 회원가입 (Firebase + 서버 동기화)
  - 로그인
  - 로그아웃
  - ID 토큰 관리
- [x] ApiService 구현 (`lib/services/api_service.dart`)
  - 자동 토큰 첨부
  - 토큰 갱신 (401 에러 시)
- [x] Validation 유틸리티 (`lib/utils/validation.dart`)
  - 이메일, 비밀번호, 사용자명, 성별 검증
- [x] 로그인 화면 (`lib/screens/login_screen.dart`)
- [x] 회원가입 화면 (`lib/screens/signup_screen.dart`)
  - GmarketSans 폰트 적용
  - 성별 선택 (RadioButton)
  - 비밀번호 확인 검증
- [x] 패키지 버전 업데이트
  - `http: ^1.6.0`
  - `lints: ^6.0.0`
  - Firebase Auth API 변경사항 대응

### Phase 2: 메인 화면 구조
- [x] **하단 탭 바 구현** (`lib/screens/main_screen.dart`)
  - 4개 탭: 옷장, 오늘의 코디, 즐겨찾는 코디, 마이페이지
  - BottomNavigationBar 구현
  - 탭 전환 기능
- [x] **main_screen.dart 연결**
  - Placeholder를 FavoritesScreen으로 교체
  - 탭 전환 기능 확인

### Phase 3: 옷장 기능

#### 디자인 가이드
- **폰트**: 
  - GmarketSans (Medium, weight: 500) - 일반 텍스트
  - Google Fonts - Aboreto - "MY CLOSET" 타이틀
- **색상**: 심플한 흰색/검은색 기반
- **스타일**: Material Design 3

#### 1. 옷장 메인 화면 (`lib/screens/closet_screen.dart`) ✅

**화면 구성:**
- [x] **AppBar**: 
  - 타이틀: "MY CLOSET" (Google Fonts - Aboreto)
  - 배경: 흰색
  - 높이: 기본 AppBar 높이
- [x] **Body**: 
  - 2x2 Grid 레이아웃 (상의, 하의, 신발, 아우터)
  - 각 카테고리 카드는 Card로 구성

**카테고리 카드 UI:**
- [x] **상단 영역**:
  - 카테고리 제목 (한글: 상의, 하의, 신발, 아우터) - GmarketSans
  - 오른쪽 화살표 아이콘 (Icons.arrow_forward_ios)
  - Row 레이아웃으로 배치
- [x] **하단 영역**:
  - 아이템이 있을 때: 최근 등록된 아이템 이미지 표시 (최대 4개)
    - `GET /api/v1/closet/{category}`로 전체 목록 가져와서 최신 4개만 표시
    - GridView로 2x2 배치
    - `cached_network_image` 패키지 사용
    - 이미지 URL: `{baseUrl}/{image_url}`
  - 아이템이 없을 때 (빈 카테고리):
    - 회색 배경 + "+" 아이콘 + "옷을 추가해보세요" 텍스트
    - 중앙 정렬

**기능:**
- [x] 카테고리 카드 탭 시 `ClosetItemsScreen`으로 네비게이션
  - 전달할 데이터: 카테고리 이름 (한글) 및 카테고리 코드 (영문: top, bottom, shoes, outer)
- [x] 로딩 상태: CircularProgressIndicator (전체 화면)
- [x] 에러 처리: 네트워크 오류 시 SnackBar 또는 에러 메시지 표시
- [x] Pull-to-refresh 기능 (RefreshIndicator)

**API 연동:**
- [x] 각 카테고리별로 `GET /api/v1/closet/{category}` 호출
- [x] 카테고리 코드 매핑: 상의(top), 하의(bottom), 신발(shoes), 아우터(outer)
- [x] 응답에서 최신 4개 아이템만 추출하여 표시

#### 2. 카테고리별 아이템 목록 화면 (`lib/screens/closet_items_screen.dart`) ✅

**화면 구성:**
- [x] **AppBar**: 
  - 왼쪽: 뒤로가기 버튼 (Icons.arrow_back)
  - 중앙: 선택한 카테고리 이름 (예: "하의") - GmarketSans
  - 오른쪽: 
    - "+" 버튼 (Icons.add) - 아이템 추가
    - 편집 버튼 (Icons.edit_square) - 편집 모드 토글
- [x] **Body**: 
  - GridView로 아이템 이미지 카드 형태로 표시 (3열)
  - 각 아이템 카드:
    - 이미지 표시 (`cached_network_image` 패키지 사용)
    - 편집 모드일 때: 선택된 아이템에 파란색 테두리 표시
    - 이미지 URL: `{baseUrl}/{image_url}`
- [x] **편집 모드 하단 버튼** (편집 모드 활성화 시에만 표시):
  - "삭제" 버튼 (왼쪽, 회색 배경)
  - "코디에 추가" 버튼 (오른쪽, 회색 배경)
  - 선택된 아이템이 없을 때는 버튼 비활성화

**기능:**
- [x] **일반 모드**:
  - 특정 카테고리의 모든 아이템 목록 표시
  - "+" 버튼 클릭: 이미지 추가 (`image_picker` 패키지 사용)
    - 갤러리에서 이미지 선택
    - 선택한 이미지를 `ClosetService.addItem()`으로 서버에 업로드
    - 성공 시 편집 모드 종료 및 목록 새로고침
- [x] **편집 모드** (편집 버튼 클릭 시):
  - 각 아이템 카드 탭 시 선택/해제 (파란색 테두리로 표시)
  - 여러 아이템 선택 가능
  - 하단에 "삭제"와 "코디에 추가" 버튼 표시
  - "삭제" 버튼:
    - 선택한 아이템들을 `ClosetService.deleteItem(itemId)`로 삭제
    - 여러 아이템 선택 시 순차적으로 삭제 처리
    - 확인 다이얼로그 표시
    - 성공 시 편집 모드 종료 및 목록 새로고침
  - "코디에 추가" 버튼:
    - 선택한 아이템을 오늘의 코디에 추가
    - `OutfitService.updateOutfitItem(category, itemId)` 호출
    - 카테고리별로 하나의 아이템만 선택 가능 (마지막 선택된 아이템이 적용)
    - 성공 시 편집 모드 종료 및 목록 새로고침
- [x] 편집 모드 종료: 편집 버튼 다시 클릭 또는 아이템 추가 후 자동 종료

**API 연동:**
- [x] `GET /api/v1/closet/{category}` - 아이템 목록 조회
- [x] `POST /api/v1/closet/{category}` - 아이템 추가 (multipart/form-data)
- [x] `DELETE /api/v1/closet/{item_id}` - 아이템 삭제
- [x] `PUT /api/v1/outfit/today` - 코디에 아이템 추가
- [x] 카테고리 코드는 이전 화면에서 전달받은 값 사용

**필요한 패키지:**
- [x] `cached_network_image` - 이미지 캐싱 및 표시
- [x] `image_picker` - 갤러리/카메라에서 이미지 선택

---

### Phase 4: 오늘의 코디 기능 ✅

#### 디자인 가이드
- **폰트**: 
  - GmarketSans (Medium, weight: 500) - 일반 텍스트
- **색상**: 심플한 흰색/검은색 기반
- **스타일**: Material Design 3

#### 1. 오늘의 코디 메인 화면 (`lib/screens/today_outfit_screen.dart`) ✅

**화면 구성:**
- [x] **AppBar**: 
  - 타이틀: "오늘의 코디" (GmarketSans, 중앙 정렬)
  - 오른쪽: "AI 추천" 버튼 (연한 보라색 배경, 둥근 사각형)
    - Material Symbols `smart_toy` 아이콘 (코드 포인트: 0xf06c, fontFamily: 'MaterialSymbolsRounded') + "AI 추천" 텍스트
- [x] **Body**: 
  - LayoutBuilder를 사용하여 화면 크기에 맞춰 동적으로 비율 계산
  - 2x2 Grid 레이아웃 (상의, 하의, 신발, 아우터)
  - 각 카테고리 카드는 Card로 구성
  - 카드 왼쪽 상단에 카테고리 제목 표시 (한글: 상의, 하의, 신발, 아우터)
  - 레이아웃 설정:
    - padding: 16.0 (좌우)
    - 상단 패딩: 24.0 (AppBar와의 간격)
    - 하단 패딩: 16.0
    - mainAxisSpacing: 24.0 (세로 간격)
    - crossAxisSpacing: 20.0 (가로 간격)
    - heightReduction: 30.0 (하단 탭 바와의 거리 조정)
    - GridView.count 사용

**카테고리 카드 UI:**
- [x] **아이템이 없을 때**:
  - 카드 중앙에 큰 검은색 "+" 아이콘 표시
  - "+" 아이콘 클릭 시 아이템 선택 모달 표시
- [x] **아이템이 있을 때**:
  - 카드에 아이템 이미지 표시 (`cached_network_image` 패키지 사용)
  - 이미지 URL: `{baseUrl}/{image_url}`
  - 이미지 클릭 시 아이템 선택 모달 표시 (다른 아이템으로 변경 가능)

**하단 버튼:**
- [x] "즐겨찾는 코디로 저장" 버튼 (어두운 회색 배경, 흰색 텍스트)
  - 클릭 시 코디 명 입력 다이얼로그 표시

**기능:**
- [x] 화면 진입 시 `OutfitService.getTodayOutfit()` 호출하여 현재 코디 조회
- [x] 각 카테고리 카드의 "+" 버튼 또는 이미지 클릭 시 아이템 선택 모달 표시
- [x] "AI 추천" 버튼 클릭 시 `OutfitService.recommendOutfit()` 호출
  - AI 추천 결과로 각 카테고리에 아이템 자동 채움
  - 성공 시 화면 새로고침
  - 빈 옷장일 때 에러 메시지 표시 (두 줄 SnackBar)
- [x] "즐겨찾는 코디로 저장" 버튼 클릭 시 코디 명 입력 다이얼로그 표시
  - 코디 명 입력 후 확인 시 `FavoritesService.saveFavorite()` 호출
  - 현재 오늘의 코디 상태를 즐겨찾기로 저장
  - 코디 미완성 시 에러 메시지 표시 (두 줄 SnackBar)
- [x] 로딩 상태: CircularProgressIndicator (전체 화면)
- [x] 에러 처리: 네트워크 오류 시 SnackBar 또는 에러 메시지 표시
- [x] Pull-to-refresh 기능 (RefreshIndicator)

**API 연동:**
- [x] `GET /api/v1/outfit/today` - 오늘의 코디 조회
- [x] `PUT /api/v1/outfit/today` - 코디 아이템 선택/변경
- [x] `POST /api/v1/outfit/recommend` - AI 추천 실행
- [x] `POST /api/v1/favorites` - 즐겨찾기 저장

#### 2. 아이템 선택 모달 (`lib/widgets/item_selection_dialog.dart`) ✅

**구현 방식:**
- [x] 커스텀 `Dialog` 위젯 사용
- [x] `showDialog`를 사용하여 화면 중앙에 모달 표시
- [x] 오늘의 코디 메인 화면 위에 오버레이 형태로 띄워짐
- [x] Dialog 크기: 화면 너비의 90%, 화면 높이의 70% 정도
- [x] Material Design 3 스타일 적용 (둥근 모서리, elevation)
- [x] Dialog 내부 구조: `Column`으로 구성
  - 상단: 헤더 (제목 + 닫기 버튼)
  - 중간: `Expanded` + `GridView` (아이템 목록)
  - 하단: 확인 버튼

**화면 구성:**
- [x] **상단**: 
  - 카테고리 제목 (예: "신발") - GmarketSans, 중앙 정렬
  - 오른쪽: 닫기 버튼 (X 아이콘)
- [x] **Body**: 
  - 해당 카테고리의 옷장 아이템 목록을 GridView로 표시
  - 각 아이템 카드:
    - 아이템 이미지 표시 (`cached_network_image` 패키지 사용)
    - 우측 상단에 체크박스 표시 (선택 가능)
    - 이미지 URL: `{baseUrl}/{image_url}`
- [x] **하단**: 
  - "확인" 버튼 (어두운 회색 배경, 흰색 텍스트)
  - 선택된 아이템이 없어도 확인 버튼을 누르면 모달이 닫힘

**기능:**
- [x] 모달 표시 시 `ClosetService.getItemsByCategory(category)` 호출하여 아이템 목록 조회
- [x] 각 아이템 카드 클릭 시 선택/해제 (체크박스 토글)
- [x] 여러 아이템 선택 가능 (하지만 코디에는 하나만 적용)
- [x] "확인" 버튼 클릭 시:
  - 선택된 아이템이 있으면:
    - 선택된 아이템 중 하나를 선택 (마지막 선택된 아이템)
    - `OutfitService.updateOutfitItem(category, itemId)` 호출
    - 성공 시 모달 닫기 및 메인 화면 새로고침
  - 선택된 아이템이 없으면:
    - 모달만 닫기 (아이템 변경 없음)
- [x] 닫기 버튼(X) 클릭 시 모달 닫기
- [x] 로딩 상태: CircularProgressIndicator
- [x] 에러 처리: 네트워크 오류 시 SnackBar 표시

**API 연동:**
- [x] `GET /api/v1/closet/{category}` - 카테고리별 아이템 목록 조회
- [x] `PUT /api/v1/outfit/today` - 선택한 아이템을 코디에 적용

#### 3. 코디 명 입력 다이얼로그 (`lib/widgets/favorite_name_dialog.dart`) ✅

**다이얼로그 구성:**
- [x] **제목**: "코디 명" (GmarketSans, 검은색)
- [x] **입력 필드**: 
  - 텍스트 입력 필드 (회색 테두리, 둥근 모서리)
  - 플레이스홀더: "코디 1" (회색 텍스트)
- [x] **하단 버튼**: 
  - 왼쪽: "취소" 버튼 (흰색 배경, 검은색 텍스트)
  - 오른쪽: "확인" 버튼 (어두운 회색 배경, 흰색 텍스트)

**기능:**
- [x] "확인" 버튼 클릭 시:
  - 입력된 코디 명 검증 (빈 문자열 체크)
  - `FavoritesService.saveFavorite(name)` 호출
  - 성공 시 다이얼로그 닫기 및 성공 메시지 표시 (SnackBar)
- [x] "취소" 버튼 클릭 시 다이얼로그 닫기
- [x] 에러 처리: 저장 실패 시 에러 메시지 표시
- [x] 재사용 가능하도록 구현 (저장/수정 모드 지원)
  - `initialName` 파라미터로 수정 모드 지원
  - `favoriteId` 파라미터로 수정 시 ID 전달
  - 다이얼로그 제목 자동 변경 ("코디 명" / "코디 명 수정")

**API 연동:**
- [x] `POST /api/v1/favorites` - 즐겨찾기 저장
- [x] `PUT /api/v1/favorites/{id}` - 즐겨찾기 이름 변경

#### 4. AI 추천 기능 ✅

**기능:**
- [x] "AI 추천" 버튼 클릭 시:
  - 로딩 표시 (CircularProgressIndicator)
  - `OutfitService.recommendOutfit()` 호출
  - AI가 추천한 Outfit 객체를 받아서 각 카테고리에 아이템 자동 채움
  - 성공 시 화면 새로고침하여 추천된 코디 표시
  - 실패 시 에러 메시지 표시 (SnackBar)
  - 빈 옷장일 때 특정 에러 메시지 표시 (두 줄 SnackBar)

**API 연동:**
- [x] `POST /api/v1/outfit/recommend` - AI 추천 실행
  - 반환: Outfit 객체 (각 카테고리별 추천 아이템 포함)

---

### Phase 5: 즐겨찾기 기능 ✅

#### 디자인 가이드
- **폰트**: 
  - GmarketSans (Medium, weight: 500) - 일반 텍스트
- **색상**: 심플한 흰색/검은색 기반
- **스타일**: Material Design 3

#### 1. 즐겨찾는 코디 메인 화면 (`lib/screens/favorites_screen.dart`) ✅

**화면 구성:**
- [x] **AppBar**: 
  - 타이틀: "즐겨찾는 코디" (GmarketSans, 중앙 정렬)
  - 편집 모드일 때만 삭제 버튼과 취소 버튼 표시
- [x] **Body**: 
  - 즐겨찾는 코디 목록을 카드 형식으로 표시
  - `ListView` 사용 (세로 스크롤)
  - Pull-to-refresh 지원 (`RefreshIndicator`)

**코디 카드 UI:**
- [x] **카드 구조**:
  - 흰색 배경, 둥근 모서리 (12px)
  - 그림자 효과 (elevation: 2)
  - 좌측 정렬
- [x] **상단 영역**:
  - 왼쪽: 코디명 (GmarketSans, 16px, 검은색)
  - 오른쪽: 편집 아이콘 (연필 아이콘, 회색)
    - 클릭 시 코디명 수정 다이얼로그 표시
    - `favorite_name_dialog.dart` 재사용 (현재 코디명이 초기값으로 입력됨)
- [x] **하단 영역**:
  - 2x2 Grid 레이아웃으로 아이템 이미지 표시
  - 각 카테고리별 아이템 이미지 (`cached_network_image` 사용)
  - 이미지 URL: `{baseUrl}/{image_url}`
  - 아이템이 없는 카테고리는 회색 배경으로 표시
- [x] **편집 모드**:
  - Long Press로 편집 모드 진입
  - 편집 모드일 때 체크박스 표시 (우측 상단)
  - 선택된 카드는 파란색 테두리 표시
  - 하단에 삭제 버튼 표시 (선택된 항목이 있을 때만)

**삭제 기능 구현:**
- [x] **Long Press (길게 누르기)**
  - 카드를 길게 누르면 편집 모드 진입
  - 첫 번째 카드 자동 선택
  - `GestureDetector`의 `onLongPress` 사용
- [x] **편집 모드**:
  - 카드 탭으로 선택/해제
  - 여러 코디 선택 가능
  - AppBar에 삭제 버튼과 취소 버튼 표시
  - 하단에 삭제 버튼 표시 (선택된 항목이 있을 때만)
  - 삭제 확인 다이얼로그 표시
  - `FavoritesService.deleteFavorite()` 호출하여 삭제
  - 성공 시 목록 새로고침 및 성공 메시지 표시

**기능:**
- [x] 화면 진입 시 `FavoritesService.getFavoritesList()` 호출하여 즐겨찾기 목록 조회
- [x] 각 코디별 `FavoritesService.getFavoriteById()` 호출 (이미지 표시용)
- [x] 편집 아이콘 클릭 시:
  - 코디명 수정 다이얼로그 표시 (`favorite_name_dialog.dart` 재사용)
  - 현재 코디명이 텍스트 필드의 초기값으로 설정됨
  - 사용자가 코디명 수정 후 확인 시 `FavoritesService.renameFavorite()` 호출
  - 성공 시 목록 새로고침
- [x] 카드 클릭 시:
  - 편집 모드가 아닐 때: 상세 다이얼로그 표시 (`favorite_detail_dialog.dart`)
  - 편집 모드일 때: 선택/해제
- [x] 카드 Long Press 시:
  - `GestureDetector`의 `onLongPress` 사용
  - 편집 모드 진입 및 첫 번째 카드 자동 선택
- [x] 빈 상태 처리:
  - 즐겨찾기가 없을 때 안내 메시지 표시
  - 하트 아이콘 + "저장된 즐겨찾기가 없습니다" + "오늘의 코디에서 마음에 드는 코디를 저장해보세요"
- [x] 로딩 상태: CircularProgressIndicator
- [x] 에러 처리: 네트워크 오류 시 SnackBar 표시

**API 연동:**
- [x] `GET /api/v1/favorites` - 즐겨찾기 목록 조회
- [x] `GET /api/v1/favorites/{id}` - 특정 코디 상세 조회 (이미지 표시용)
- [x] `PUT /api/v1/favorites/{id}` - 코디 이름 변경
- [x] `DELETE /api/v1/favorites/{id}` - 코디 삭제

#### 2. 코디명 수정 다이얼로그 ✅

**구현 방식:**
- [x] `lib/widgets/favorite_name_dialog.dart` 재사용
- [x] 다이얼로그 생성 시 현재 코디명을 초기값으로 전달
- [x] 텍스트 필드에 현재 코디명이 미리 입력되어 있음
- [x] 사용자가 수정 후 확인 시 `FavoritesService.renameFavorite(id, newName)` 호출

**기능:**
- [x] 다이얼로그 표시 시 현재 코디명이 텍스트 필드에 표시됨
- [x] "확인" 버튼 클릭 시:
  - 입력된 코디 명 검증 (빈 문자열 체크)
  - `FavoritesService.renameFavorite(id, newName)` 호출
  - 성공 시 다이얼로그 닫기 및 성공 메시지 표시 (SnackBar)
- [x] "취소" 버튼 클릭 시 다이얼로그 닫기
- [x] 에러 처리: 이름 변경 실패 시 에러 메시지 표시

**API 연동:**
- [x] `PUT /api/v1/favorites/{id}` - 코디 이름 변경
  - 요청 본문: `{ "new_name": "새로운 코디 명" }`

#### 3. 코디 상세 다이얼로그 (`lib/widgets/favorite_detail_dialog.dart`) ✅

**구현 방식:**
- [x] 커스텀 `Dialog` 위젯 사용
- [x] `showDialog`를 사용하여 화면 중앙에 모달 표시
- [x] Material Design 3 스타일 적용 (둥근 모서리, elevation)

**화면 구성:**
- [x] **헤더**: 
  - 코디명 표시 (GmarketSans, 20px, 굵게)
  - 오른쪽: 닫기 버튼 (X 아이콘)
- [x] **Body**: 
  - 2x2 Grid 레이아웃으로 아이템 이미지 표시
  - 각 카테고리별 아이템 이미지 (`cached_network_image` 사용)
  - 아이템이 없는 카테고리는 회색 배경 + 카테고리명 표시
- [x] **하단**: 
  - "닫기" 버튼 (어두운 회색 배경, 흰색 텍스트)

**기능:**
- [x] 카드 클릭 시 상세 다이얼로그 표시
- [x] `FavoritesService.getFavoriteById()` 호출하여 상세 정보 조회
- [x] 로딩 상태: CircularProgressIndicator
- [x] 에러 처리: 네트워크 오류 시 SnackBar 표시

---

## 📋 앞으로 해야 할 작업


---

### Phase 6: 마이페이지 기능

> 구성 가이드 추후 작성 예정

---

### Phase 7: 인증 상태 관리
- [ ] 인증 상태 Provider/Notifier 구현
- [ ] main.dart 라우팅 수정 (로그인 여부에 따른 화면 분기)
- [ ] 자동 로그인 기능

### Phase 8: 최종 점검
- [ ] 전체 플로우 테스트
- [ ] UI/UX 개선
- [ ] 에러 처리 보완

---

## 📝 참고사항

### 메인 화면 구조
```
MainScreen (하단 탭 바)
├── 옷장 화면 (index 0)
├── 오늘의 코디 화면 (index 1)
├── 즐겨찾는 코디 화면 (index 2)
└── 마이페이지 화면 (index 3)
```
---

## 🔧 기술 스택
- Flutter SDK (>=3.0.0 <4.0.0)
- Firebase Core ^4.2.0
- Firebase Auth ^6.1.0
- HTTP ^1.6.0
- HTTP Parser ^4.0.2
- Google Fonts ^6.1.0
- Image Picker ^1.2.1
- Cached Network Image ^3.4.1
- Lints ^6.0.0 (dev)

**폰트:**
- GmarketSans (Medium, weight: 500)
- Material Symbols (Rounded, Outlined, Sharp)

---

**마지막 업데이트:** 2025-11-28

## 📊 진행 상황 요약

- ✅ **Phase 1**: Firebase 인증 및 기본 화면
- ✅ **Phase 2**: 메인 화면 구조
- ✅ **Phase 3**: 옷장 기능
- ✅ **Phase 4**: 오늘의 코디 기능
- ✅ **Phase 5**: 즐겨찾기 기능
- ⏳ **Phase 6**: 마이페이지 기능 (구성 가이드 추후 작성)
- ⏳ **Phase 7**: 인증 상태 관리
- ⏳ **Phase 8**: 최종 점검

